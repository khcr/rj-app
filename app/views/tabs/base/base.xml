<Page loaded="loaded" xmlns:navbar="views/components/navbar">
  <!-- ActionBar -->
  <Page.actionBar>
      <ActionBar>
        <StackLayout orientation="horizontal" horizontalAlignment="center">
          <Image src="res://logorjpr" width="40px"></Image>
        </StackLayout>
      </ActionBar>
  </Page.actionBar>

  <GridLayout columns="*" rows="*, 60">

    <!-- Content -->
    <ScrollView>
      <StackLayout id="container">

      </StackLayout>
    </ScrollView>


    <!-- Navbar -->
    <StackLayout row="1" id="navbar" orientation="horizontal">

      <button id="toFeed" class="nav" tap="toFeed">
        <button.formattedText>
            <formattedString>
              <formattedString.spans>
                  <span text="&#xE0E5;&#x000a;" fontFamily="MaterialIcons-Regular" fontSize="35" />
                  <span text="À la une" />
              </formattedString.spans>
            </formattedString>
        </button.formattedText>
      </button>
      <button id="toAgenda" class="nav" tap="toAgenda">
        <button.formattedText>
            <formattedString>
              <formattedString.spans>
                  <span text="&#xE192;&#x000a;" fontFamily="MaterialIcons-Regular" fontSize="35" />
                  <span text="Programme" />
              </formattedString.spans>
            </formattedString>
        </button.formattedText>
      </button>
      <button id="toTestimonies" class="nav" tap="toTestimonies">
        <button.formattedText>
            <formattedString>
              <formattedString.spans>
                  <span text="&#xE87D;&#x000a;" fontFamily="MaterialIcons-Regular" fontSize="35" />
                  <span text="Témoignage" />
              </formattedString.spans>
            </formattedString>
        </button.formattedText>
      </button>
      <button id="toPlus" class="nav" tap="toPlus">
        <button.formattedText>
            <formattedString>
              <formattedString.spans>
                  <span text="&#xE8EE;&#x000a;" fontFamily="MaterialIcons-Regular" fontSize="35" />
                  <span text="Plus" />
              </formattedString.spans>
            </formattedString>
        </button.formattedText>
      </button>
    </StackLayout>



  </GridLayout>

</Page>
