<StackLayout id="navbar" loaded="loaded" orientation="horizontal">

  <button id="feed" class="nav" tap="toFeed">
    <button.formattedText>
        <formattedString>
          <formattedString.spans>
              <span text="&#xE0E5;&#x000a;" fontFamily="MaterialIcons-Regular" fontSize="35" />
              <span text="À la une" />
          </formattedString.spans>
        </formattedString>
    </button.formattedText>
  </button>
  <button id="agenda" class="nav" tap="toAgenda">
    <button.formattedText>
        <formattedString>
          <formattedString.spans>
              <span text="&#xE192;&#x000a;" fontFamily="MaterialIcons-Regular" fontSize="35" />
              <span text="Programme" />
          </formattedString.spans>
        </formattedString>
    </button.formattedText>
  </button>
  <button id="testimonies" class="nav" tap="toTestimonies">
    <button.formattedText>
        <formattedString>
          <formattedString.spans>
              <span text="&#xE87D;&#x000a;" fontFamily="MaterialIcons-Regular" fontSize="35" />
              <span text="Témoignage" />
          </formattedString.spans>
        </formattedString>
    </button.formattedText>
  </button>
  <button id="plus" class="nav" tap="toPlus">
    <button.formattedText>
        <formattedString>
          <formattedString.spans>
              <span text="&#xE8EE;&#x000a;" fontFamily="MaterialIcons-Regular" fontSize="35" />
              <span text="Plus" />
          </formattedString.spans>
        </formattedString>
    </button.formattedText>
  </button>
</StackLayout>
