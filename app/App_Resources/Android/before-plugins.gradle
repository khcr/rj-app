android {
  // Match the google play services & firebaseMessaging version
  // to resolve a conflict between Google Maps SDK & Push Plugin
  project.ext {
    googlePlayServicesVersion = "12.0.1"
    firebaseMessagingVersion = "12.0.1"
  }
}